.flex.flex-row.items-center
  %h1.mr-2
    = link_to @artist.name, [@artist], class: :link
    = "-"
    = @track.name
  = link_to [:edit, @artist, @track], class: "btn btn-sm btn-ghost" do
    = image_tag "edit.svg", class: "w-4 h-4"
    ред
  .flex-auto.text-right
    = link_to [@artist], class: "btn btn-sm btn-ghost" do
      = image_tag "arrow_left.svg", class: "h-4 w-4"
      назад
  
- if @track.description.present?
  = raw @track.description

%hr

.flex.flex-row.items-center.py-2
  %h2.mr-2.flex-row Играет в рассказах

%hr.py-2/

- if @stories.present?
  .overflow-x-auto
    %table.table.w-full
      %thead
        %tr
          %th Название
      %tbody
        - @stories.each do |story|
          %tr
            %td
              = link_to [story.author, story] do
                = "#{story.author.name} - #{story.name} (#{story.completed ? 'Полностью' : story.playlists_count})"

- else
  %h2 Этот трек пока не играет ни в одном рассказе.


-# = audio_tag "http://mds.mds-club.ru/Slushatel_Svetlana_-_Perimetr.mp3", autoplay: true, controls: true, data: { turbo_permanent: true }
