<% if @new_artist %>
  window.location.href = "<%= artist_track_path(@new_artist, @track)%>";
<% else %>
  $("#modal_window").html("<%= j render('move_form')%>");
<% end %>
$('#modal_window').modal('toggle');
