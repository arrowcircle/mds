.flex.flex-row.items-center.align-center
  .flex-1
    %h1.mr-2
      Участники проекта
%hr.my-2

- if @users.present?
  .grid.grid-cols-1{ class: "md:grid-cols-2 gap-2 md:gap-4" }
    = render @users

  - if @pagy.pages > 1
    .flex.flex-col.items-center.my-2
      = raw pagy_nav(@pagy)
- else
  %h3 Таких участников нет :( Попробуй поменять что-то.
