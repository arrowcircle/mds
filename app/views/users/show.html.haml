%h1= @user.username
= "#{time_ago_in_words(@user.created_at)} на сайте"

- if @user.findings_count > 0
  %h3 Опознал треков: #{@user.findings_count}

- if @user.requests_count > 0
  %h3 Последние запросы: #{@user.requests_count}
