- if @artists.empty?
  .hero-unit
    %h2 Такого исполнителя у нас нет.
    %p
      Попробуйте поиск по первым двум букавам имени исполнителя.
      %br
      Если Вы ничего не найдете, просто добавьте исполнителя сами - это просто!
    %p
      = link_to "Добавить", new_author_path, :class => 'btn btn-primary'
- else
  %table.table.table-striped
    %thead
    %tbody
      = render @artists
  = paginate @artists