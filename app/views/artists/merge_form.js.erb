<% if @new_artist %>
  window.location.href = "<%= artist_tracks_path(@new_artist)%>";
<% else %>
  $("#modal_window").html("<%= j render('merge_form')%>");
<% end %>
$('#modal_window').modal('toggle');
